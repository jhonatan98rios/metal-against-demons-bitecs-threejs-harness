/**
 * Renderable component for BitECS.
 * This component determines whether an entity should be rendered in the entity-component-system.
 *
 * @property {number[]} isRenderable - Flag indicating if the entity should be rendered (1 = renderable, 0 = not renderable)
 */
export const Renderable = {
  isRenderable: [] as number[]
}
